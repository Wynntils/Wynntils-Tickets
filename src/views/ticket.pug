extends default

block content
    div.ticket
        each chat in chatlog.logs
            p.chatline
                span.user #{chat.user}
                - if (typeof(chat.deleted) !== 'undefined'){
                s #{chat.content}
                -}else{
                |  #{chat.content}
                -}